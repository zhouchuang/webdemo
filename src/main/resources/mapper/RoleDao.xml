<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd"><mapper namespace="user.zc.dao.base.RoleDao" ><!-- 查询字段 --><sql id="columnList" >	r.id,	 r.name, r.createBy, r.createTime, r.updateBy, r.updateTime, r.version, r.isDeleted, r.status, r.description</sql><!--通过id集合in查询--><sql id="inClause">    <if test="column!=null">        <if test="ids==null">            and 1=0        </if>        <if test="ids!=null">            <if test="ids.size>0">                and  r.${column} in                <foreach item="items" index="index" collection="ids" open="(" separator="," close=")">                    #{items}                </foreach>            </if>            <if test="ids.size==0">                and 1=0            </if>        </if>    </if></sql><!-- 查询条件 --><sql id="whereClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and r.id = #{condition.id}	</if>	<if test="condition.name != null" >and  r.name = #{condition.name}</if><if test="condition.createBy != null" >and  r.createBy = #{condition.createBy}</if><if test="condition.createTime != null" >and  r.createTime = #{condition.createTime}</if><if test="condition.updateBy != null" >and  r.updateBy = #{condition.updateBy}</if><if test="condition.updateTime != null" >and  r.updateTime = #{condition.updateTime}</if><if test="condition.version != null" >and  r.version = #{condition.version}</if><if test="condition.isDeleted != null" >and  r.isDeleted = #{condition.isDeleted}</if><if test="condition.status != null" >and  r.status = #{condition.status}</if><if test="condition.description != null" >and  r.description = #{condition.description}</if></trim><include refid="inClause"/></sql><!-- 模糊查询条件 --><sql id="whereLikeClause">where 1=1<trim  suffixOverrides="," >	<if test="condition.id != null" >	    and r.id = #{condition.id}	</if>													 			<if test="condition.name != null" >and  UPPER(r.name) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.name}),'%'))</if><if test="condition.createBy != null" >and  UPPER(r.createBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createBy}),'%'))</if><if test="condition.createTime != null" >and  UPPER(r.createTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.createTime}),'%'))</if><if test="condition.updateBy != null" >and  UPPER(r.updateBy) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateBy}),'%'))</if><if test="condition.updateTime != null" >and  UPPER(r.updateTime) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.updateTime}),'%'))</if><if test="condition.version != null" >and  UPPER(r.version) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.version}),'%'))</if><if test="condition.isDeleted != null" >and  UPPER(r.isDeleted) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.isDeleted}),'%'))</if><if test="condition.status != null" >and  UPPER(r.status) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.status}),'%'))</if><if test="condition.description != null" >and  UPPER(r.description) LIKE   UPPER(CONCAT(CONCAT('%',#{condition.description}),'%'))</if></trim><include refid="inClause"/></sql><!-- 插入记录 --><insert id="add"  >  insert into role (id,name,createBy,createTime,updateBy,updateTime,version,isDeleted,status,description)  values(#{id,jdbcType=VARCHAR},#{name},#{createBy},#{createTime},#{updateBy},#{updateTime},#{version},#{isDeleted},#{status},#{description})</insert><!-- 修改记录，只修改只不为空的字段 --><update id="update" >	update role set	<trim  suffixOverrides="," >	<if test="name != null ">name=#{name},</if><if test="createBy != null ">createBy=#{createBy},</if><if test="createTime != null ">createTime=#{createTime},</if><if test="updateBy != null ">updateBy=#{updateBy},</if><if test="updateTime != null ">updateTime=#{updateTime},</if><if test="version != null ">version=#{version},</if><if test="isDeleted != null ">isDeleted=#{isDeleted},</if><if test="status != null ">status=#{status},</if><if test="description != null ">description=#{description},</if>	</trim> where id=#{id} and version = #{version}-1</update><!-- 删除记录 --><delete id="del" >	delete from role where id = #{id}</delete><!-- 根据id查询  --><select id="get"   resultType="Role"  parameterType="String">	select <include refid="columnList" /> 	from role  r	where  r.id = #{id}  and r.isDeleted = false</select><!-- 列表总数--><select id="count" resultType="java.lang.Long" >	select count(1) 	from role  r	<include refid="whereClause"/></select><!-- 查询page --><select id="findPage" resultType="Role" >	select 	<include refid="columnList"/>	from role r	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 模糊查询page --><select id="findLikePage" resultType="Role" >	select 	<include refid="columnList"/>	from role r	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询列表list --><select id="findList" resultType="Role" >	select	<include refid="columnList"/>	from role r	<include refid="whereClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!--模糊查询list--><select id="findLikeList" resultType="Role" >	select	<include refid="columnList"/>	from role r	<include refid="whereLikeClause"/>	<if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合条件的数据的id集合 --><select id="findIds" resultType="String">	select r.${idsColumn}	from role r    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 查询符合模糊条件的数据的id集合 --><select id="findLikeIds" resultType="String">	select r.${idsColumn}	from role r    <include refid="whereLikeClause"/>    <if test="orderByClause!=null">${orderByClause}</if></select><!-- 获取一个对象通过非id --><select id="one" resultType="Role" >     select    <include refid="columnList"/>     from role r    <include refid="whereClause"/>    <if test="orderByClause!=null">${orderByClause}</if>    limit 0,1</select><!--如果你想返回一个HashMap 就实现这个select标签--><select id="queryHashMap" resultType="java.util.HashMap"  parameterType="Object"></select><select id="findRolesByAccountNo"  resultType="Role"   parameterType="String">	select r.name,r.id from user u	left join userRole ur on u.id = ur.userId	left join role r on r.id = ur.roleId	where  u.accountNo =  #{accountNo}</select></mapper>